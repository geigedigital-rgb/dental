{
  "name": "dental-warehouse",
  "private": true,
  "description": "Склад стоматологической клиники: backend (NestJS) + frontend (Vite/React). Один деплой — бэкенд отдаёт API и статику.",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build && cd .. && mkdir -p backend/public && cp -r frontend/dist/* backend/public/",
    "start": "cd backend && npm run start:prod",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
