# База данных (обязательно). Для Neon: ?connection_limit=5 в конце URL при таймаутах.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

# Секрет для JWT (обязательно в продакшене). Локально можно не менять.
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Порт сервера (опционально). Railway подставляет PORT сам.
PORT=3001

# Разрешённые origins для CORS, через запятую. Пусто = разрешить все (удобно для локальной разработки).
# В продакшене укажите URL фронтенда, например: https://your-app.railway.app
CORS_ORIGIN=""

# development | production (опционально). Локально по умолчанию development.
NODE_ENV="development"
